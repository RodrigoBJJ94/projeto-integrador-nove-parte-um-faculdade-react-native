import { StyleSheet } from "react-native";

export default StyleSheet.create({
    viewCaption: {
        alignItems: "center",
        marginTop: 50,
        marginBottom: 100,
    },
    caption: {
        fontSize: 24,
        color: "#000000",
        fontWeight: "bold"
    }
});
