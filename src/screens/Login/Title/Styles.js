import { StyleSheet } from "react-native";

export default StyleSheet.create({
    title: {
        color: "#000000",
        fontSize: 26,
        textAlign: "center",
        marginTop: 120,
        fontWeight: "bold"
    }
});
