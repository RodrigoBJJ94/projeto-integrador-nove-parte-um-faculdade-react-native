import { StyleSheet } from "react-native";

export default StyleSheet.create({
    title: {
        color: "#000000",
        fontSize: 24,
        textAlign: "center",
        marginTop: 100,
        fontWeight: "bold"
    }
});
