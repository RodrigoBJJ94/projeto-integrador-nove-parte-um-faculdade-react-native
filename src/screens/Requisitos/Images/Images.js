import React from "react";
import { View, Text, TouchableOpacity } from "react-native";


export default function Images({ captureScreen }) {
    return (
        <View>
         
            
        </View>
    );
};
